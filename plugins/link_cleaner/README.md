# Link Cleaner

Privacy by default: every link you send is scrubbed of tracking parameters
(`utm_*`, `fbclid`, `gclid`, `si`, `igshid`, …) before it leaves the device.

The link itself and all non-tracking parameters are preserved.
