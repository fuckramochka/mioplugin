# Code Formatter

Markdown shortcuts for outgoing messages:

- `.code python` + newline + code — fenced block with language
- `.inline text` — `monospace`
- `.spoiler text` — spoiler
- `.quote line1\nline2` — quoted lines
